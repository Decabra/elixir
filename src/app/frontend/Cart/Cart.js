import React from 'react';
import PropTypes from 'prop-types';
import './Cart.scss';

const Cart = () => (
  <div className="Cart" data-testid="Cart">
    Cart Component
  </div>
);

Cart.propTypes = {};

Cart.defaultProps = {};

export default Cart;
